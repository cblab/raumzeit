# Archived root-level legacy artifacts

This folder stores root-level legacy files that are not part of the canonical execution path:

- `Flow.ipynb` (historical notebook)
- `Info.md` (historical narrative/bootstrap notes)

Canonical execution truth is the modular engine under `emergent-geometry-causal-graphs/` using `src/`, `configs/`, and `scripts/`.
